name: Build Android
on:
  workflow_dispatch:
  push:
    branches: [main]
concurrency:
  group: android-build
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "9.4.1"
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"
      - name: Re-encode videos for broad Android compatibility
        run: |\n          sudo apt-get update\n          sudo apt-get install -y ffmpeg\n          for video in app/src/main/res/raw/exercise_march_arm_sweep.mp4 app/src/main/res/raw/exercise_band_pull_apart.mp4; do\n            ffmpeg -y -i "$video" -an -vf "scale=1280:720:flags=lanczos,format=yuv420p" -c:v libx264 -profile:v baseline -level 3.1 -preset medium -crf 23 -movflags +faststart "${video%.mp4}.compatible.mp4"\n            mv "${video%.mp4}.compatible.mp4" "$video"\n          done\n      - name: Build APK and release AAB\n        run: gradle :app:assembleDebug :app:bundleRelease
      - uses: actions/upload-artifact@v4
        with:
          name: azara-strong-android-builds
          path: |
            app/build/outputs/apk/debug/app-debug.apk
            app/build/outputs/bundle/release/app-release.aab
